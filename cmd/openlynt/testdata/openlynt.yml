rule_one:
  type: import
  require:
    # eg, /pkg/(1: prefix)/v(2: 12)\
    regexp: \/pkg\/(?P<prefix>[a-z0-9]+)\/v(?P<version>[0-9]+)

    name:
      # eg "pkgPREFIXv1"
      template: pkg{{ "${prefix}" | upper }}v${version}


